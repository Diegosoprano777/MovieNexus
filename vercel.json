{
  "buildCommand": "cd MovieNexus && npm install && npm run build",
  "outputDirectory": "MovieNexus/dist/MovieNexus/browser",
  "installCommand": "echo done",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
